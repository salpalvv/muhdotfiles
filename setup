sudo apt install neovim python-pip python3-pip fonts-hack-ttf gcc make git tlp bumblebee rxvt-unicode-256color i3 xbacklight

pip3 install neovim
pip install neovim

#sudo apt install blender
#git clone https://github.com/maestrogerardo/i3-gaps-deb

make world

#sudo update-alternatives --config x-terminal-emulator
